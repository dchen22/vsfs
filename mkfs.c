#include "mkfs.h"

int format_disk(const char *disk_name, size_t disk_size, size_t max_files) {
    // Implementation of disk formatting logic
    printf("Formatting disk: %s\n", disk_name);
    
    


    return 0; // Return 0 on success
}